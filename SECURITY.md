# 安全策略

## 这个工具会接触哪些数据

给上报者一个清晰的边界，方便判断某件事算不算安全问题：

**会读取**

- Jellyfin 的 `jellyfin.db`（**只读**：`mode=ro` + `PRAGMA query_only=ON`）
- 你配置的媒体目录中的文件名、大小、时长、编解码器信息（通过 ffprobe）
- 你主动填写的服务器地址 / API Key / 本地 LLM 地址

**会写入**

- 数据目录下的配置、SQLite 数据库、JSON 日志（见 README「数据与隐私」）
- 你授权执行操作的媒体文件（修复、转码、合并、回收站删除）—— 且都会先备份

**不会做**

- 不联网上报。没有任何遥测、统计、崩溃上报、内置第三方域名。
- 唯一的出站请求是**你自己填的**地址：Jellyfin 服务器、本地 LLM 接口、
  以及「检测可用模型」时读的 `<你的 LLM 地址>/api/tags`。
- 不写入 `jellyfin.db`，不修改 Jellyfin 的配置文件。
- 不以任何形式收集、上传或外传媒体库内容、路径清单或观看记录。

如果你发现代码里存在**未在文档中说明的**网络出口、遥测、或对用户数据的越界读写，
那确实是安全问题，请按下面方式反馈。

## 上报漏洞

请**不要**开公开 issue。

优先使用 GitHub 的私密上报通道：

> 仓库页面 → **Security** 标签 → **Report a vulnerability**

或者直接发邮件给维护者（见仓库提交历史里的作者邮箱）。

请尽量包含：

- 影响的版本（`python main.py --version`）
- 复现步骤或最小复现片段
- 你观察到的实际行为，以及你预期的行为
- 是否能在**干净环境**（无个人配置）下复现

## 响应

这是个人维护的开源项目，没有 SLA。但涉及以下两类会优先处理：

1. **数据丢失 / 破坏用户文件**（P0）
2. **隐私泄露 / 未声明的网络出口**（P0）

修复后会发补丁版本，并在 `CHANGELOG.md` 里说明。

## 不在范围内

- 你需要先在本地装好 FFmpeg、Python 等依赖才能运行 —— 依赖自身的漏洞请报给上游
- 被安全软件误报（未签名的 PyInstaller 产物常见）—— 可以在 Release 页面对照我们公布的 SHA256
- 你使用本工具管理的媒体内容本身的合法性问题 —— 那是使用者自己的责任

## 给使用者的建议

- 从 [Releases](https://github.com/coldeve2022/JellyfinToolkit/releases) 下载，
  并用页面上给出的 SHA256 校验压缩包。
- 首次使用前备份媒体库。
- API Key 存在本地 `config.json` 里（明文）。不要把 `config.json` 分享出去，
  也不要把它放进任何同步网盘或仓库。
